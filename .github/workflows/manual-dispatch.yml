on: 
  workflow_dispatch:
    inputs:
      environment:
        description: 'Define env name'     
        required: true
        default: 'test'
      branch:
        description: 'Define branch name'     
        required: true
        default: 'main'
